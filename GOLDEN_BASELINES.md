# GOLDEN BASELINES REGISTRY

> status: production-governance
> purpose: register immutable quality baselines used for regression and promotion decisions
> current_s3_owner: `skills/prose-preparation/SKILL.md`
> current_s3_route: `skills/prose-preparation/routes/s3-source-shadow.md`

## GB-DIRECT-SOURCE-V2_15-001

```text
NAME: Direct Source Slot Fill v2.15 Golden Baseline
STATUS: GOLDEN_BASELINE_LOCKED
BRANCH: backup/direct-source-slot-fill-v2.15-good
IMMUTABLE_ANCHOR_COMMIT: 28c17928f29623a88897a4daf109c4bb6fea8971
BASELINE_MANIFEST: GOLDEN_BASELINE.md on the backup branch
AUTHOR_TASTE_APPROVAL: PASS
CURRENT_PRODUCTION_ROLE: Source Shadow high-homology primitive / prose-feel anchor
```

### What it proves

这份 Golden 仍然证明一个核心事实：

```text
human donor prose kept open
→ direct slot replacement
→ source-only deletion
→ minimum target-only insertion
→ minimal grammar repair
→ target prose
```

它现在**不是独立生产 route**。

正式映射已经升级为：

```text
S3
→ source acquisition / fidelity
→ Source Shadow scene-window selection
→ high homology:
     Direct Source Slot Fill primitive
→ partial homology:
     copy-weighted scene-window transduction
→ story/source-leak/reader-trust validation
→ prose candidate
```

不存在独立 S4。

### Copy policy update

旧 Golden 中“anti-copy”不再拥有生产路由权，也不得为了降低相似度主动同义词化。

当前原则：

```text
HIGH SOURCE-NATIVE WORDING OVERLAP: legal
SOURCE FACT / WORLD / CHARACTER LEAK: fail
WRONG TARGET STORY: fail
UNNECESSARY SYNONYMIZATION: fail
```

即：能沿用的母本普通词、短语、句架和段落推进允许沿用；真正禁止的是把 Source 专属剧情事实抄错进 Target。

### Retired mappings

```text
independent Golden Direct Edit route
S3A / S3B route select
S3 preparation-only → S4 render
S4 prose generation / validation
Scene Fabric production route
sentence-shell production route
GROK_NOVEL_V1 production render
```

这些只保留历史证据，不得恢复为当前 production route。

### Promotion / regression rule

当前 production truth 以：

```text
AGENTS.md
NOVEL_WORKFLOW_ENTRY.md
PRODUCTION_CONTRACT.md
skills/PRODUCTION_MAP.md
skills/prose-preparation/SKILL.md
```

为准。

历史 Golden 可以作为质量比较和回归锚点，但**不得反向覆盖当前 Source Shadow 路由**。

最低回归关注：

```text
EVENT_FACT_FIDELITY: PASS
SOURCE_FACT_LEAK: 0
LEXICAL_SOURCE_FIDELITY: not materially worse
PLOT_VISIBILITY: PASS
BASE_MODEL_DICTION_REBOUND: false
```

## Governance memory line

> **Golden Direct Source 现在是 Source Shadow 的高同构执行原语与质量锚点，不是第二条正文路线；S3 唯一 production route 是 Source Shadow。**
