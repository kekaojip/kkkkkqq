# Book Construction local authority router

> applies_to: Book Construction v2.4+
> role: Stage 1 local authority router

Before Stage 1 execution：

```text
read SKILL.md
then load only references required by current Stage 1 phase
```

For Foundation / Current Arc boundary work, load：

```text
references/stage1-story-state-non-instantiation.md v1.1+
```

This file routes authority. It does not permanently override stale duplicate algorithms.

Canonical boundary：

```text
Stage 1
= book identity
+ protagonist core
+ mechanism / world physics
+ long-form direction
+ Current Arc directional constraints
+ anti-cheat boundaries
+ explicit author locks

Stage 1
!= opening reality constructor
!= Target Macro story constructor
```

Hard distinction：

```text
STAGE1_LONG_FORM_DIRECTION
!= STAGE2_TARGET_MACRO_STORY
```

Unless explicitly author-locked, Stage 1 must leave opening-local facts：

```text
UNINSTANTIATED_BY_STORY
```

including opening place, occupation, routine, housing, local relationships, resources, first-scene container and first concrete incident.

Long-term story directions such as supporting lines / stage directions / protagonist journey are author-owned creative decisions, not ordinary AI-autofill details.

Conflict between current Stage 1 Owners is a bug：

```text
STAGE1_POLICY_DRIFT: FAIL
→ repair the stale owning rule
→ do not solve by adding another permanent override
```

Canonical shorthand：

> **Stage 1 defines what the book is and where it may grow. Stage 2 Story Room decides what actually happens.**